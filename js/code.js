var eye = document.getElementById("eye");
var eye = document.getElementById("input");

eye.addEventListener("click", function() {
    if(input.type == "password"){
        input.type = text = "text"
        eye.style.opacity = 0.8
    }else{
        input.type ="password"
        eye.style.opaacity = 0.2
    }
})
